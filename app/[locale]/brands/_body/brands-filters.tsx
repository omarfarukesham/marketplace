'use client';

import SideFilters from '@/app/_components/layout/side-filters/side-filters';

const BrandsFilters = ({ isDesktop }: { isDesktop: boolean }) => {
  return (
    <SideFilters isDesktop={isDesktop}>
      {/* Todo: Add here */}
      <div></div>
    </SideFilters>
  );
};

export default BrandsFilters;
